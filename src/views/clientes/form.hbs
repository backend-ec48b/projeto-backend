<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Cliente</title>
    <style>
        body {
            height: 90vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-around;
            padding: 2rem;
            gap: 1rem;
            margin: 0;
            box-sizing: border-box;
        }

        
        .container{
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 90%;
            max-width: 700px;
            background-color: #fff;
            padding: 5rem 3rem;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            gap: 2rem;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 2rem;
            width: 100%;
        }

        label {
            font-weight: 600;
            color: #333;
        }

        .input-info{
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        button {
            text-decoration: none;
            background-color: #0066cc;
            color: white;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            transition: background-color 0.2s;
        }
        
        button:hover {
            background-color: #005bb5;
        }

        a {
            text-decoration: none;
            color: #0066cc;
        }
        
        a.button-link {
            text-decoration: none;
            background-color: #6c757d;
            color: white;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            text-align: center;
            transition: background-color 0.2s;
        }
        
        a.button-link:hover {
            background-color: #5a6268;
        }

        .container-info{
            display: flex;
            flex-direction: column;
            align-items: start;
            justify-content: space-around;
        }

        input{
            width: 100%;
            border-radius: 6px;
            padding: 0.6rem;
            border: 1px solid #ccc;
            transition: border-color 0.2s;
            box-sizing: border-box;
        }
        
        input:focus {
            outline: none;
            border-color: #0066cc;
        }

        .actions-btn{
            display: flex;
            align-items: center;
            gap: 1rem;
            justify-content: flex-start;
        }

        .form-section {
            border: 1px solid #eee;
            border-radius: 8px;
        }

        .form-header {
            background-color: #0066cc;
            color: white;
            padding: 0.75rem 1.5rem;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        
        .form-header.secundario {
            background-color: #6c757d;
        }
        
        .form-body {
            padding: 0 1.5rem 1.5rem 1.5rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        
        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
            padding: 0.75rem 1.25rem;
            border-radius: 0.25rem;
            width: 90%;
            max-width: 700px;
        }
        
        small {
            font-size: 0.8rem;
            color: #6c757d;
            margin-top: 5px;
        }

    </style>
</head>
<body>

    {{#if erro}}
        <div class="alert-danger" role="alert">
            {{erro}}
        </div>
    {{/if}}

    <div class="container">
        <form action="{{action}}" method="POST" novalidate>
            
            <div class="form-section">
                <div class="form-header">Dados Pessoais</div>
                <div class="form-body">
                    
                    <div class="input-info">
                        <label for="cpf">CPF</label>
                        <input 
                            type="text" 
                            id="cpf" 
                            name="cpf" 
                            value="{{#if cliente._id}}{{cliente._id}}{{else}}{{cliente.cpf}}{{/if}}" 
                            required 
                            placeholder="Apenas números (11 dígitos)"
                            pattern="\d{11}" 
                            maxlength="11" 
                            title="O CPF deve conter exatamente 11 dígitos (apenas números)."
                            {{#if cliente._id}}readonly{{/if}}>
                        
                        {{#if cliente._id}}
                            <small>O CPF não pode ser alterado.</small>
                        {{/if}}
                    </div>
                    
                    <div class="input-info">
                        <label for="nome">Nome</label>
                        <input type="text" id="nome" name="nome" 
                            value="{{cliente.nome}}" required>
                    </div>
                    
                    <div class="input-info">
                        <label for="email">E-mail</label>
                        <input type="email" id="email" name="email" 
                            value="{{cliente.email}}" required>
                    </div>
                </div>
            </div>

            <div class="form-section">
                <div class="form-header secundario">Endereço</div>
                <div class="form-body">
                    
                    <div class="input-info">
                        <label for="rua">Rua</label>
                        <input type="text" id="rua" name="endereco[rua]" 
                            value="{{cliente.endereco.rua}}" required>
                    </div>
                    
                    <div class="input-info">
                        <label for="cidade">Cidade</label>
                        <input type="text" id="cidade" name="endereco[cidade]" 
                            value="{{cliente.endereco.cidade}}" required>
                    </div>

                    <div class="input-info">
                        <label for="logradouro">Logradouro</label>
                        <input type="text" id="logradouro" name="endereco[logradouro]" 
                            value="{{cliente.endereco.logradouro}}" required>
                    </div>
                </div>
            </div>

            <div class="actions-btn">
                <button type="submit">
                    Salvar
                </button>
                <a href="/clientes" class="button-link">
                    Cancelar
                </a>
            </div>
        </form>
    </div>

</body>
</html>