<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if item}}Editar Pedido #{{item._id}}{{else}}Criar Novo Pedido{{/if}}</title>
    <style>
        body {
            height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            margin: 0;
            box-sizing: border-box;
        }

        .container {
            background-color: #fff;
            padding: 3rem;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 650px;
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        h1 {
            color: #333;
            font-size: 2rem;
            border-bottom: 1px solid #eee;
            padding-bottom: 1rem;
            margin-bottom: 1rem;
        }

        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
            padding: 1rem;
            border-radius: 4px;
            width: 100%;
            text-align: left;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        label {
            font-weight: 600;
            color: #333;
            display: block;
            margin-bottom: 0.5rem;
        }

        input[type="text"], textarea {
            width: 100%;
            border-radius: 6px;
            padding: 0.75rem;
            border: 1px solid #ccc;
            transition: border-color 0.2s;
            box-sizing: border-box;
            font-family: inherit;
        }
        
        input:focus, textarea:focus {
            outline: none;
            border-color: #0066cc;
        }
        
        small {
            display: block;
            margin-top: 0.25rem;
            color: #6c757d;
            font-size: 0.85rem;
        }

        .actions-btn-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 1rem;
        }

        button[type="submit"] {
            background-color: #0066cc;
            color: white;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        button[type="submit"]:hover {
            background-color: #005bb5;
        }

        .btn-secondary {
            text-decoration: none;
            background-color: #6c757d;
            color: white;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            transition: background-color 0.2s;
        }
        
        .btn-secondary:hover {
            background-color: #5a6268;
        }
    </style>
</head>
<body>

<div class="container">
    
    <h1>{{#if item}}Editar Pedido #{{item._id}}{{else}}Criar Novo Pedido{{/if}}</h1>
    
    {{#if error}}
        <div class="alert-danger" role="alert">
            Erro: {{error}}
        </div>
    {{/if}}

    <form method="POST" action="/pedidos/{{#if item}}{{item._id}}{{/if}}" novalidate>

        {{#if item}}
            <div>
                <label for="notaFiscal">Nota Fiscal (ID):</label>
                <input 
                    type="text" 
                    id="notaFiscal" 
                    name="notaFiscal" 
                    value="{{item._id}}"
                    required 
                    readonly 
                >
            </div>
        {{/if}}

        <div>
            <label for="clienteCpf">CPF do Cliente (Somente Números):</label>
            <input 
                type="text" 
                id="clienteCpf" 
                name="clienteCpf" 
                value="{{#if item}}{{item.clienteCpf}}{{else}}{{dadosAnteriores.clienteCpf}}{{/if}}"
                placeholder="Ex: 12345678900 (11 dígitos)"
                required 
                oninput="this.value = this.value.replace(/\D/g, '').slice(0, 11)" 
                inputmode="numeric"
                pattern="\d{11}" 
                title="O CPF deve conter exatamente 11 dígitos numéricos."
            >
            <small>Digite apenas os 11 dígitos do CPF, sem pontos ou traços.</small>
        </div>

        <div>
            <label for="produtosNome">Nomes dos Produtos (Separados por vírgula):</label>
            <textarea 
                id="produtosNome" 
                name="produtosNome" 
                rows="4" 
                placeholder="Ex: Produto A, Produto B, Produto C"
                required
            >{{#if item}}{{#each item.produtos}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}{{else}}{{dadosAnteriores.produtosNome}}{{/if}}</textarea>
        </div>

        <div class="actions-btn-container">
            <button type="submit">
                {{#if item}}Atualizar Pedido{{else}}Criar Pedido{{/if}}
            </button>
            <a href="/pedidos/listar" class="btn-secondary">Cancelar e Voltar</a>
        </div>
        
    </form>
</div>

</body>
</html>