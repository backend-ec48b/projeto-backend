<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Gerenciamento Estilizado</title>
    <style>
        body {
            height: 90vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 2rem;
            margin: 0; 
            box-sizing: border-box; 
            width: 100vw;
        }

        main{
            background-color: #fff;
            padding: 3rem;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            display: flex;
            flex-direction: column;
            width: 100%; 
            max-width: 1000px;
        }

        label {
            font-weight: 600;
            color: #333;
        }

        a {
            text-decoration: none;
            background-color: #0066cc;
            color: white;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            border-radius: 8px;
            display: inline-block; 
            text-align: center;
        }

        .container-info{
            display: flex;
            flex-direction: column;
            align-items: start;
            justify-content: space-around;
        }
        
        nav {
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            background-color: #fff;
            border-bottom: 1px solid #ddd;
            width: 100%;
            padding: 10px 0;
            margin-bottom: 20px; 
        }

        .logo a {
            font-size: 1.5em; 
            font-weight: bold; 
            text-decoration: none; 
            color: #333 !important; 
            background-color: transparent !important;
            padding: 0 !important;
            border-radius: 0 !important;
        }

        .auth-links a {
            background-color: transparent !important;
            padding: 8px 12px !important; 
            border: 1px solid;
            font-weight: 400; 
            margin-left: 10px;
        }
        
        .auth-links a[href="/clientes/logout"] {
            color: #d9534f !important;
            border-color: #d9534f !important;
        }

        .auth-links a[href="/clientes/login"] {
            color: #5cb85c !important;
            border-color: #5cb85c !important;
        }

        .auth-links a[href="/clientes/cadastrar"] {
            color: #337ab7 !important;
            border-color: #337ab7 !important;
        }

        .auth-links span {
            color: #555;
            margin-right: 15px;
        }
        
        header {
            width: 100;
            padding: 20px 0;
            text-align: center;
        }
        
        h1 {
            color: #333;
            font-size: 2em;
            margin-bottom: 10px;
        }
        
        section h2 {
            margin-top: 30px;
            color: #0066cc;
        }

        .module-card {
            border: 1px solid #ccc; 
            padding: 15px; 
            flex: 1;
            border-radius: 6px;
        }

        .module-card h3 {
            color: #333;
            margin-top: 0;
        }
        
        .module-card a {
            margin-top: 10px;
        }
        
        .module-card.meus-pedidos {
            border: 1px solid #4f46e5; 
            background-color: #e0f2fe;
        }
        
        .module-card.meus-pedidos a {
            background-color: #4f46e5 !important;
            color: white !important;
        }
        
        hr {
            border: 0;
            border-top: 1px solid #eee;
            margin: 20px 0;
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">
            <a href="/">Ecommerce</a>
        </div>

        <div class="auth-links">
            {{#if usuario}}
                <span>Bem-vindo, 
                    <strong>
                        {{#if usuario.nome}}
                            {{usuario.nome}}
                        {{else}}
                            {{usuario.cpf}}
                        {{/if}}
                        
                        {{#if isAdmin}}
                            (Administrador)
                        {{else}}
                            (Cliente)
                        {{/if}}
                    </strong>
                </span>
                <a href="/clientes/logout">Logout</a>
            {{else}}
                <a href="/clientes/login">Login</a>
                <a href="/clientes/cadastrar">Cadastrar</a>
            {{/if}}
        </div>
    </nav>

    <main>
        <header>
            <h1>Bem-vindo ao Sistema de Gerenciamento de um Ecommerce</h1>
            <p>Utilize os links abaixo para acessar e gerenciar os dados da aplicação.</p>
        </header>

        <hr>

        <section>
            <h2>Módulos de Gestão</h2>
            <div style="display: flex; gap: 20px; margin-top: 20px;">
                
                {{#if isAdmin}}
                <div class="module-card">
                    <h3>Clientes</h3>
                    <p>Gerenciamento completo de informações de clientes.</p>
                    <a href="/clientes">Acessar Clientes</a>
                </div>
                {{/if}}
                
                {{#if usuario}}
                <div class="module-card">
                    <h3>Produtos</h3>
                    <p>Visualização do catálogo de produtos disponíveis.</p>
                    <a href="/produtos">Acessar Produtos</a>
                </div>
                {{/if}}
                
                {{#if isAdmin}}
                <div class="module-card">
                    <h3>Gestão de Pedidos</h3>
                    <p>Visualização e gestão do fluxo de pedidos e vendas.</p>
                    <a href="/pedidos/listar">Acessar Pedidos (Admin)</a>
                </div>
                {{/if}}
                
                {{#if usuario}}
                    {{#unless isAdmin}}
                    <div class="module-card meus-pedidos">
                        <h3>Meus Pedidos 🛒</h3>
                        <p>Acompanhe o status e faça a gestão dos seus pedidos.</p>
                        <a href="/pedidos">Acessar Meus Pedidos</a>
                    </div>
                    {{/unless}}
                {{/if}}
                
            </div>
        </section>

        <hr>

        {{#if isAdmin}}
        <section>
            <h2>Resumo do Sistema</h2>
            <div style="margin-top: 10px;">
                <p>Total de Clientes Registrados: <strong>{{stats.totalClientes}}</strong></p>
                <p>Você tem <strong>{{stats.produtosAtivos}}</strong> produtos ativos em estoque.</p>
            </div>
        </section>
        {{/if}}
    </main>
</body>
</html>