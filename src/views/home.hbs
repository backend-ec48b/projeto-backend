<nav style="display: flex; justify-content: space-between; align-items: center; padding: 10px; background-color: #f4f4f4; border-bottom: 1px solid #ddd;">
    <div class="logo">
        <a href="/" style="font-size: 1.5em; font-weight: bold; text-decoration: none; color: #333;">Meu App</a>
    </div>

    <div class="auth-links">
        {{#if usuario}}
            <span style="margin-right: 15px;">Bem-vindo, 
                <strong>
                    {{#if usuario.nome}}
                        {{usuario.nome}}
                    {{else}}
                        {{usuario.cpf}}
                    {{/if}}
                    
                    {{#if isAdmin}}
                        (Administrador)
                    {{else}}
                        (Cliente)
                    {{/if}}
                </strong>
            </span>
            <a href="/clientes/logout" style="text-decoration: none; color: #d9534f; padding: 8px 12px; border: 1px solid #d9534f; border-radius: 4px;">Logout</a>
        {{else}}
            <a href="/clientes/login" style="text-decoration: none; color: #5cb85c; margin-right: 10px; padding: 8px 12px; border: 1px solid #5cb85c; border-radius: 4px;">Login</a>
            <a href="/clientes/cadastrar" style="text-decoration: none; color: #337ab7; padding: 8px 12px; border: 1px solid #337ab7; border-radius: 4px;">Cadastrar</a>
        {{/if}}
    </div>
</nav>

<header style="padding: 20px 0;">
    <h1>Bem-vindo ao Sistema de Gerenciamento de um Ecommerce</h1>
    <p>Utilize os links abaixo para acessar e gerenciar os dados da aplicação.</p>
</header>

<hr>

<section>
    <h2>Módulos de Gestão</h2>
    <div style="display: flex; gap: 20px; margin-top: 20px;">
        
        {{#if isAdmin}}
        <div style="border: 1px solid #ccc; padding: 15px; flex: 1;">
            <h3>Clientes</h3>
            <p>Gerenciamento completo de informações de clientes.</p>
            <a href="/clientes">Acessar Clientes</a>
        </div>
        {{/if}}
        
        {{#if usuario}}
        <div style="border: 1px solid #ccc; padding: 15px; flex: 1;">
            <h3>Produtos</h3>
            <p>Visualização do catálogo de produtos disponíveis.</p>
            <a href="/produtos">Acessar Produtos</a>
        </div>
        {{/if}}
        
        {{#if isAdmin}}
        <div style="border: 1px solid #ccc; padding: 15px; flex: 1;">
            <h3>Gestão de Pedidos</h3>
            <p>Visualização e gestão do fluxo de pedidos e vendas.</p>
            <a href="/pedidos/listar">Acessar Pedidos (Admin)</a>
        </div>
        {{/if}}
        
        {{#if usuario}}
            {{#unless isAdmin}}
            <div style="border: 1px solid #4f46e5; padding: 15px; flex: 1; background-color: #e0f2fe;">
                <h3>Meus Pedidos 🛒</h3>
                <p>Acompanhe o status e faça a gestão dos seus pedidos.</p>
                <a href="/pedidos" style="color: #4f46e5; font-weight: bold;">Acessar Meus Pedidos</a>
            </div>
            {{/unless}}
        {{/if}}
        
    </div>
</section>

<hr>

{{#if isAdmin}}
<section>
    <h2>Resumo do Sistema</h2>
    <div style="margin-top: 10px;">
        <p>Total de Clientes Registrados: <strong>{{stats.totalClientes}}</strong></p>
        <p>Você tem <strong>{{stats.produtosAtivos}}</strong> produtos ativos em estoque.</p>
    </div>
</section>
{{/if}}